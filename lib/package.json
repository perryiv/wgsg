{
  "name": "wgsg-lib",
  "description": "WebGPU Scene Graph Library",
  "author": "Perry L Miller IV",
  "license": "MIT",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clear": "clear",
    "build": "tsc --build && vite build",
    "lint": "eslint --ext .ts ./src",
    "prepublishOnly": "yarn run build",
    "watch": "watch \"yarn run clear && yarn run lint && yarn run build && echo watching ...\" src"
  },
  "dependencies": {
    "gl-matrix": "^3.4.3"
  },
  "devDependencies": {
    "@eslint/js": "^9.19.0",
    "@webgpu/types": "^0.1.56",
    "clear-cli": "^2.0.0",
    "eslint": "^9.19.0",
    "globals": "^15.14.0",
    "http-server": "^14.1.1",
    "typescript": "~5.7.2",
    "typescript-eslint": "^8.29.0",
    "vite": "^6.2.4",
    "vite-plugin-dts": "^4.5.3",
    "watch": "^1.0.2"
  }
}
